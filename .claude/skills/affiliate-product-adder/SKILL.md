---
name: affiliate-product-adder
description: Use this skill when the user gives an Amazon product URL and wants it added to lovelytools.ai as a new affiliate product page. Triggers on phrases like "add this product", "create an affiliate page for [Amazon link]", "add [product] to the site". Fetches the product data from Amazon, builds an AffiliateProduct entry matching the site's design system, and adds it to the product store so it renders through AffiliateProductTemplate.
---

# Affiliate Product Adder

Adds a new Amazon-sourced affiliate product to lovelytools.ai, rendered through
the existing `AffiliateProductTemplate` component so every product page looks
identical in structure and matches the site's real design tokens
(`packages/ui/src/styles/tokens/colors.css`), in both light and dark mode.

## Files this skill relies on

- `lib/affiliate-products.ts` — the `AffiliateProduct` type + the array new entries get appended to
- `components/templates/affiliate-product-template.tsx` — the page layout (already built, matches Claude Design's "Affiliate Product Page" prototype). Do not edit this file when adding a product — only add data.
- `app/products/[slug]/page.tsx` — the route that renders any product added to the store, at `/products/{slug}`

## Steps

1. **Get the affiliate link**
   - Extract the ASIN from the given Amazon URL (the `/dp/{ASIN}/` or `/gp/product/{ASIN}/` segment).
   - If the URL has no visible ASIN (e.g. a `/hz/mobile/mission/` redirect link), open it in the browser tool and read the resolved URL's ASIN instead.
   - Note the Amazon domain (`amazon.co.uk`, `amazon.com`, etc.) from the URL.
   - Affiliate tag is `lovelytools-21`.

2. **Fetch the product data**
   Open the canonical product page (`https://www.{domain}/dp/{ASIN}`) with the browser tool and extract:
   - Product title → split into `brand` (first word/brand name) and `name` (rest of the title)
   - Price (for reference only — the template always sends users to Amazon for current price, per the design; don't hardcode price on the page)
   - Main product image + up to 4 additional gallery images
   - Bullet points / "About this item" → source material for `specs`, `features`, `pros`
   - Category breadcrumb on the Amazon page → informs `categoryLabel` (a reasonable guess is fine; the user will fix this once the real category exists)

3. **Download images locally**
   Save the product images under `public/products/{slug}/` (create the folder). Reference them in the entry as `/products/{slug}/1.jpg` etc. Never hotlink Amazon's own image URLs — they can break or change.

4. **Write the copy**
   Fill in, in the user's/brand voice, matching the tone of the existing example entry (VOCOO Kitchen Timer — practical, specific, no hype-filler):
   - `tagline` — one punchy sentence
   - `description` — 1–2 sentences on the core problem/benefit
   - `specs` — 6–8 short label/value pairs from the listing
   - `features` — 4–6 short feature callouts with a lucide-react icon name each
   - `pros` / `cons` — grounded in the actual listing and reviews, not invented
   - `bestFor` / `notIdealFor` — who this product suits
   - `score` — an honest 0–10 based on the listing's rating/review signal (do not inflate)
   - `verdict` — 1–2 sentence summary
   - `faq` — 3–4 Q&As sourced from the listing's actual Q&A section or common bullet points

5. **Generate the slug**
   Kebab-case of brand + core product name (e.g. `vocoo-kitchen-timer`). Check `allAffiliateProductSlugs()` in `lib/affiliate-products.ts` to avoid collisions.

6. **Append the entry**
   Add the new `AffiliateProduct` object to the `affiliateProducts` array in `lib/affiliate-products.ts`. Do not touch any other array entries.

7. **Verify**
   Run `pnpm dev` (or confirm the user already has it running) and check `/products/{slug}` renders correctly in both light and dark mode — the template pulls dark/light entirely from the site's existing theme toggle, no extra work needed.

8. **Report back**
   Tell the user the new page URL (`/products/{slug}`) and flag anything you had to guess (category label, score) so they can review it.

## What NOT to do

- Don't invent specs, pros/cons, or FAQ answers not grounded in the actual Amazon listing.
- Don't modify `affiliate-product-template.tsx` per-product — all customization is data, not markup, so every product page stays visually consistent.
- Don't hardcode price — Amazon prices change; the template always routes to Amazon for current price.
- Don't skip the affiliate tag — every outbound link must go through `affiliateUrl()` in `lib/affiliate-products.ts`, never a raw Amazon URL.
